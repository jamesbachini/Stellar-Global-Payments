# ==============================================================================
# Stellar Global Payments - Backend Configuration
# ==============================================================================
#
# Most configuration is automatically loaded from:
#   ../shared/config/accounts.local.json (generated by ./deploy.sh)
#
# This file only requires SECRETS that should never be committed to git.
# ==============================================================================

# REQUIRED: Admin account secret key (hot wallet that submits transactions)
# Generate with: stellar keys generate admin --network testnet
ADMIN_SECRET_KEY=SA...YOUR_SECRET_KEY_HERE

# REQUIRED: API authentication token for admin-only endpoints
# Generate with: openssl rand -hex 32
ADMIN_AUTH_TOKEN=your_random_secure_token_here

# ==============================================================================
# OPTIONAL OVERRIDES (defaults loaded from shared/config/accounts.local.json)
# ==============================================================================

# Server port (default: 4000)
# PORT=4000

# Override RPC URL (default: from shared config)
# SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Override admin public key (default: from shared config)
# ADMIN_PUBLIC_KEY=GD...YOUR_PUBLIC_KEY

# Override USDC contract (default: from shared config)
# USDC_CONTRACT_ID=CB...USDC_CONTRACT_ID

# Override explorer URL (default: auto-detected from network)
# EXPLORER_BASE_URL=https://stellar.expert/explorer/testnet/tx/
