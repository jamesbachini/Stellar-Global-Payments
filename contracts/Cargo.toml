[package]
name = "remittance_accounts"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.3.0"
openzeppelin-stellar-accounts = "0.5.0"
serde = { version = "1", default-features = false, features = ["derive"] }
serde_json = "1"
